# Local development server port (used by `npm run dev`)
PORT=3000

# Optional: override the FFmpeg binary path.
# By default the app uses `ffmpeg-static`.
# Examples:
# - macOS/Linux: /usr/local/bin/ffmpeg
# - Windows: C:\\ffmpeg\\bin\\ffmpeg.exe
# FFMPEG_PATH=
